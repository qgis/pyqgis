services:
  - docker

language: python

dist: trusty
sudo: false
cache:
  pip: true

python:
  - "3.6"

branches:
  except:
    - gh-pages

git:
  depth: 1

env:
  - QGIS_VERSION_BRANCH=master

script:
  - ./scripts/ci/run-docker.sh
